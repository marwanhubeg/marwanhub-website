#!/bin/bash

# سكريبت تشغيل لوحة التحكم

cd "$(dirname "$0")"

echo "👑 تشغيل لوحة تحكم ماروان هوب..."
echo "================================"

# التحقق من الملفات
if [ ! -f "index.html" ]; then
    echo "❌ ملف index.html غير موجود!"
    exit 1
fi

if [ ! -f "server.py" ]; then
    echo "❌ ملف server.py غير موجود!"
    exit 1
fi

# بدء الخادم
echo "🚀 بدء خادم لوحة التحكم..."
python3 server.py
